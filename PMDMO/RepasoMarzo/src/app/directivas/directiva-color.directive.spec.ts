import { DirectivaColorDirective } from './directiva-color.directive';

describe('DirectivaColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaColorDirective();
    expect(directive).toBeTruthy();
  });
});
