import { PasarMayusculasPipe } from './pasar-mayusculas.pipe';

describe('PasarMayusculasPipe', () => {
  it('create an instance', () => {
    const pipe = new PasarMayusculasPipe();
    expect(pipe).toBeTruthy();
  });
});
