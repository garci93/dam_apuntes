import { InsertarEspaciosPipe } from './insertar-espacios.pipe';

describe('InsertarEspaciosPipe', () => {
  it('create an instance', () => {
    const pipe = new InsertarEspaciosPipe();
    expect(pipe).toBeTruthy();
  });
});
