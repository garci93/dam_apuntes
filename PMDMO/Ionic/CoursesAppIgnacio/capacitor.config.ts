import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '1-CoursesApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
