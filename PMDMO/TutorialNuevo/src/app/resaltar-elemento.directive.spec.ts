import { ResaltarElementoDirective } from './resaltar-elemento.directive';

describe('ResaltarElementoDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarElementoDirective();
    expect(directive).toBeTruthy();
  });
});
