import { PasarEnlaceDirective } from './pasar-enlace.directive';

describe('PasarEnlaceDirective', () => {
  it('should create an instance', () => {
    const directive = new PasarEnlaceDirective();
    expect(directive).toBeTruthy();
  });
});
